[project]
name = "pokemon-game-of-life"
version = "0.1.0"
description = "Build tooling for the Pokemon Game of Life static site"
requires-python = ">=3.12"
dependencies = [
    "pillow>=11.0.0",
    "requests>=2.32.0",
]

[tool.uv]
package = false
